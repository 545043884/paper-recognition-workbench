# PaddleOCR device: gpu or cpu
OCR_DEVICE=gpu

# Maximum number and size of pages accepted by one request
OCR_MAX_FILES=20
OCR_MAX_FILE_SIZE_MB=15

# Vue development server allowed to call this API
OCR_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://127.0.0.1:5174
